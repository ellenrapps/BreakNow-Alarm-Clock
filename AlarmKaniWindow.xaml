<Window x:Class="alarm_vs_preview61825at113pm.AlarmKaniWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:alarm_vs_preview61825at113pm"
        mc:Ignorable="d"
        Title="AlarmKaniWindow"
        Height="250" 
        Width="355"        
        WindowStyle="None"        
        AllowsTransparency="True"
        ResizeMode="NoResize"
        Background="Transparent"
        WindowStartupLocation="CenterScreen"
        >
    <WindowChrome.WindowChrome>
        <WindowChrome CaptionHeight="0" ResizeBorderThickness="10" />
    </WindowChrome.WindowChrome>

    <Border BorderBrush="#414850" BorderThickness="12,35,12,12">
        <Grid Background="LightBlue">
            <Button x:Name="AlarmNoticeXButton" FontSize="18" Content="X" Foreground="Red" Background="#414850" Height="27" Click="AlarmNoticeXButton_Click" Margin="272,-31,4,207"/>
            <Button x:Name="AlarmNoticeOkButton" Padding="0,-2,0,0" FontSize="18" Content="OK" Click="AlarmNoticeOkButton_Click" Margin="274,170,10,9" />
            <Image Source="/Images/Bell0.png" Height="57" Margin="111,4,103,142" />
            <TextBox 
                x:Name="Mb1_0Tb" 
                Text="" 
                IsReadOnly="True"
                Background="Transparent"
                BorderBrush="Transparent"
                Margin="28,66,31,38" 
                TextWrapping="Wrap" 
                FontSize="13.5" 
                TextAlignment="Center"
                FontWeight="DemiBold"
                />
        </Grid>
    </Border>
</Window>
